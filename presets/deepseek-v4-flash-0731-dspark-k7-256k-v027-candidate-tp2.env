# =============================================================================
# DeepSeek-V4-Flash-0731 — native DSpark k=7, 256K, vLLM 0.27. ACTIVE PRODUCTION.
# (TP2, dual DGX Spark).
#
# Status:               ACTIVE PRODUCTION since 2026-08-15 (task B4.4C).
#                       Promoted from the B4.4A/B4.4B-validated candidate; the
#                       filename retains its original "-candidate-" naming from
#                       implementation (task B4.4B) to avoid an unnecessary
#                       rename across cross-referencing docs during promotion --
#                       this Status field, not the filename, is authoritative.
#                       Primary rollback:
#                       presets/deepseek-v4-flash-dspark-k7-64k-production-tp2.env
#                       (vLLM 0.25.0, MAX_MODEL_LEN=65536, production 2026-07-22
#                       to 2026-08-15). Legacy rollback:
#                       presets/deepseek-v4-flash-mtp1-production-tp2.env.
#                       Full operations doc: docs/deepseek-v4-production.md.
# Validated by:         B4.3S (correctness) - B4.3Z (256K/soak) - B4.4A
#                       (prewarm design + promotion-readiness audit) - B4.4B
#                       (repository implementation, live-validated) - B4.4C
#                       (formal promotion).
#                       Final classification: B4_4C_FORMALLY_PROMOTED_AND_PUBLISHED.
# Long-context cap:     MAX_MODEL_LEN=262144 (256K), validated B4.3W/X (single-
#                       needle + combined-pressure correctness, ~496K aggregate
#                       active tokens, four-cycle no-cooldown 128K x4 soak).
# Concurrency:          MAX_NUM_SEQS=1 (this is the approved DEFAULT). A
#                       separate MAX_NUM_SEQS=4 profile is validated but
#                       deliberately NOT the default -- see
#                       presets/deepseek-v4-flash-0731-dspark-k7-256k-v027-ms4-optional-tp2.env
#                       and docs/deepseek-v4-v027-b43s-promotion-candidate.md
#                       (Residual Risk R1).
# Rollback:             presets/deepseek-v4-flash-dspark-k7-64k-production-tp2.env
#                       (vLLM 0.25.0, primary rollback) is the authoritative
#                       rollback for this now-active-production route.
#                       presets/deepseek-v4-flash-mtp1-production-tp2.env is
#                       the legacy second-tier fallback. Neither was modified
#                       by this promotion.
#
# Image (immutable; VLLM_IMAGE below is digest-pinned):
#   ghcr.io/bjk110/vllm-spark@sha256:7a005243701c5df6f8945ea56d509f747f2c63ecff6091a0169d8109a736d09f
#   local image ID sha256:a7f0f4b8a508c0b2510fc7e4dcb916491efa03c380c9c7b84dddd4c16ad6f38d,
#   identical on spark01/spark02 and unchanged since B4.3S. The same manifest is also published as
#   v027-ngc2607-dsv4-runtime-base[-20260828] for fast thin-derivative builds; this is a DSV4-specific
#   frozen build base, not a model-neutral stack. See docs/deepseek-v4-v027-runtime-build-base.md.
#   vLLM 0.27 (NGC 26.07 line), native DSpark k=7 greedy, linux/arm64.
#
# Runtime contract:
#   graph: target FULL_DECODE_ONLY, cudagraph_capture_sizes single size 8, 2 warmups.
#   speculative: method=dspark, num_speculative_tokens=7, draft_sample_method=greedy.
#   memory: fixed KV 10 GiB FP8 (kv-cache-dtype fp8); prefix caching disabled;
#           gpu-memory-utilization 0.87; max-num-batched-tokens 8192.
#   MoE backend: marlin.
#   MANDATORY VLLM_USE_DEEP_GEMM_E8M0=1 (SM121 numerical contract, not tuning).
#
# Startup prewarm (B4.4B): use together with
#   compose/deepseek-v4/docker-compose.v027-b43s-candidate.yml, which adds a
#   `head` healthcheck and a one-shot `prewarm` service running
#   scripts/prewarm_dsv4_v027_b43s.py automatically once head is healthy.
#   Do not shorten the script's 61,440-token Target B -- an earlier ~8,300-
#   token hypothesis was tested and disproven in B4.4A; see the script's own
#   docstring and docs/deepseek-v4-v027-b43s-promotion-candidate.md.
#
# Operational recovery: after shutdown, large UVM allocations may NOT be fully
#   released -- FULL RECOVERY MAY REQUIRE REBOOT (established throughout B4.3S-Z).
#
# Validation provenance: docs/deepseek-v4-v027-b43s-promotion-candidate.md.
# =============================================================================
VLLM_IMAGE=ghcr.io/bjk110/vllm-spark@sha256:7a005243701c5df6f8945ea56d509f747f2c63ecff6091a0169d8109a736d09f
ENTRYPOINT_FILE=./entrypoints/entrypoint.sh
MODEL_PATH=/home/bjk110/Documents/Models/deepseek-ai/deepseek-ai_DeepSeek-V4-Flash-0731
MODEL_CONTAINER_PATH=/models/DeepSeek-V4-Flash-0731
SERVED_MODEL_NAME=deepseek-ai/DeepSeek-V4-Flash-0731
CLUSTER_MODE=dual-rdma
TP_SIZE=2
HEAD_ROCE_IP=10.10.10.1
WORKER_ROCE_IP=10.10.10.2
ROCE_IF_NAME=enp1s0f0np0
IB_HCA_NAME=rocep1s0f0
DISTRIBUTED_BACKEND=mp
MASTER_PORT=29500
HOST_PORT=8000
MAX_MODEL_LEN=262144
MAX_NUM_SEQS=1
GPU_MEMORY_UTILIZATION=0.87
MAX_NUM_BATCHED_TOKENS=8192
VLLM_EXTRA_ARGS=--no-enable-prefix-caching --kv-cache-memory-bytes 10737418240 --kv-cache-dtype fp8 --skip-mm-profiling --moe-backend marlin --speculative-config {"method":"dspark","num_speculative_tokens":7,"draft_sample_method":"greedy"} --compilation-config {"cudagraph_mode":"FULL_DECODE_ONLY","cudagraph_capture_sizes":[8],"cudagraph_num_of_warmups":2}
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
NCCL_IB_DISABLE=0
NCCL_DEBUG=WARN
NCCL_NVLS_ENABLE=0
VLLM_USE_BREAKABLE_CUDAGRAPH=0
VLLM_USE_DEEP_GEMM_E8M0=1
VLLM_MOE_USE_DEEP_GEMM=0
VLLM_MARLIN_USE_ATOMIC_ADD=0
VLLM_SKIP_INIT_MEMORY_CHECK=0
VLLM_SPARK_SKIP_FIXED_KV_PROFILE_RUN=0
VLLM_USE_RAY_V2_EXECUTOR_BACKEND=1
