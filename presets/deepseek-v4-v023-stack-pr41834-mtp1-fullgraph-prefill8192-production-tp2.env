# =============================================================================
# deepseek-ai/DeepSeek-V4-Flash — SM12x MTP+FULL graph PREFILL max-num-batched-tokens=8192 @131K (PR #41834)
#
# Status: PRODUCTION  (repository production package status ONLY)
#   This file is the PRODUCTION preset for the validated DeepSeek-V4-Flash
#   prefill8192 configuration. Repository production status DOES NOT activate the
#   runtime. The live serving runtime is NOT started by this file. Live activation
#   requires the separate, explicitly-approved maintenance-window procedure in
#   docs/deepseek-v4-prefill8192-production-runbook.md.
#
# Promotion / provenance:
#   - promoted from (validated candidate):
#       presets/deepseek-v4-v023-stack-pr41834-mtp1-fullgraph-prefill8192-validated-candidate-tp2.env
#       candidate SHA-256 ed13ee5e17668509f8e08072bcc206981bf43f497121380dc0000d780aa1f88a
#   - promotion / package date: 2026-06-25 (homeserver local)
#   - The runtime configuration below is byte-identical to the validated candidate
#     (header comments and status/provenance metadata differ; no runtime delta).
#
# Image / model / source identity (must match on both nodes):
#   - VLLM_IMAGE: vllm-spark:v023-stack-dsv4-sm12x-pr41834-exp-72261a7-tvmfam019
#   - image config:   sha256:4c41950c47ecb771eead3e32147a750240d6605d15ee99c54b43e0a85ea26105
#   - image manifest: sha256:2f4a96283fc5b491d5e28cee607525e32e914615bb469978beb2336cf8e62c44
#   - source pin:     72261a7af149fa5d3fe2ed2b9956e92590731012
#   - model: deepseek-ai/DeepSeek-V4-Flash (official checkpoint, 46 shards)
#
# Approved operating envelope (do not exceed without re-validation):
#   - concurrency 1 ONLY (single in-flight request; >1 NOT validated)
#   - prompts up to 131,072 tokens; typical output allowance 128 tokens
#   - fixed KV 4 GiB (4,294,967,296 bytes), KV dtype fp8; prefix cache DISABLED
#   - MTP n=1; graph FULL_DECODE_ONLY; cudagraph capture [2]
#   - TP=2 multiprocessing, one rank per physical node; transport NET/IB over RoCE
#   - EP off, B12X off, Ray off
#   Runtime KV headroom MUST be revalidated before any concurrency increase or context
#   expansion: measured KV capacity at 8192 is 159,445 tokens (1.18x @135,168), margin over
#   the largest 131K sequence ~28,274 tokens (~21.56%).
#
# Clean-start conditions (NOT automated by this preset):
#   - require MemAvailable >= 110 GiB per Spark node before full model load
#   - clean reboot of both Spark nodes is required before full model load when UVM is retained
#   - clear ONLY the dedicated vLLM cache (./.cache/vllm) on both nodes; do not delete
#     model, tokenizer, AOT, raw results, or unrelated caches
#   - default FlashMLA backend; single-node TP=1 is impossible
#
# Operator-facing requirements (mitigations M1/M2):
#   - OpenWebUI backend timeout: AIOHTTP_CLIENT_TIMEOUT >= 180 seconds required;
#       confirmed effective value 240 seconds
#   - monitoring: Prometheus and both Spark node-exporters healthy required;
#       confirmed status VLLM_MONITORING_CONFIGURED_WITH_LOG_GAPS
#   - log-only operator checks (no confirmed exported Prometheus metric):
#       graph fallback, graph recapture, per-rank health, NCCL errors, CUDA errors
#
# Rollback chain (no image rebuild required):
#   1. committed 2048 validated baseline:
#        presets/deepseek-v4-v023-stack-pr41834-mtp1-fullgraph-validated-tp2.env
#   2. graph-only L1:
#        presets/deepseek-v4-v023-stack-pr41834-fullgraph-validated-rollback-tp2.env
#   3. eager U0-RDMA L2:
#        presets/deepseek-v4-v023-stack-pr41834-eager-u0-rollback-tp2.env
#
# Runtime activation requires separate explicit approval (see production runbook).
# =============================================================================

VLLM_IMAGE=vllm-spark:v023-stack-dsv4-sm12x-pr41834-exp-72261a7-tvmfam019
ENTRYPOINT_FILE=./entrypoints/entrypoint-rdma.sh
MODEL_PATH=/home/bjk110/Documents/Models/deepseek-ai/DeepSeek-V4-Flash
MODEL_CONTAINER_PATH=/models/DeepSeek-V4-Flash
SERVED_MODEL_NAME=deepseek-ai/DeepSeek-V4-Flash
CLUSTER_MODE=dual-rdma
TP_SIZE=2
HEAD_ROCE_IP=10.10.10.1
WORKER_ROCE_IP=10.10.10.2
ROCE_IF_NAME=enp1s0f0np0
IB_HCA_NAME=rocep1s0f0
RAY_PORT=6379
DISTRIBUTED_BACKEND=mp
MASTER_PORT=29500
HOST_PORT=8000
MAX_MODEL_LEN=135168
MAX_NUM_SEQS=1
GPU_MEMORY_UTILIZATION=0.87
MAX_NUM_BATCHED_TOKENS=8192
VLLM_EXTRA_ARGS=--no-enable-prefix-caching --kv-cache-memory-bytes 4294967296 --kv-cache-dtype fp8 --skip-mm-profiling --speculative-config {"method":"deepseek_mtp","num_speculative_tokens":1} --compilation-config {"cudagraph_mode":"FULL_DECODE_ONLY","cudagraph_capture_sizes":[2],"cudagraph_num_of_warmups":2}
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
NCCL_IB_DISABLE=0
NCCL_DEBUG=WARN
NCCL_NVLS_ENABLE=0
NCCL_NET=
VLLM_USE_BREAKABLE_CUDAGRAPH=0
VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS=1
FLASHINFER_CUDA_ARCH_LIST=12.1
VLLM_USE_RAY_V2_EXECUTOR_BACKEND=0
VLLM_NCCL_SO_PATH=/usr/lib/aarch64-linux-gnu/libnccl.so.2
