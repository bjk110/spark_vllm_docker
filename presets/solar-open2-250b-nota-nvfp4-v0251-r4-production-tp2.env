# =============================================================================
# Solar-Open2-250B Nota NVFP4 — vLLM 0.25.1 r4 PRODUCTION preset.
#
# Status:      PRODUCTION. Promoted from the validated ACTIVE TEST BASELINE
#              (presets/solar-open2-250b-nota-nvfp4-v0251-r4-active-test-tp2.env,
#              preserved unchanged as historical validation provenance — do
#              not delete or repurpose it).
# Promoted:    2026-08-09, readiness state
#              SOLAR_OPEN2_V0251_R4_BF16_PRODUCTION_READY, evidence
#              solar-open2-v0251-r4-bf16-production-fasttrack-20260808T153704Z
#              (all six fast-track gates PASS: C2 targeted KDA correctness;
#              24/24 correctness suite; 200/200 mixed stability soak;
#              six-case matched performance; dual-node cold start; rollback
#              validation). Promotion evidence:
#              solar-open2-v0251-r4-bf16-production-promotion-20260809T053857Z
# Model:       nota-ai/Solar-Open2-250B-Nota-NVFP4
#              pinned HF revision de88f6226788077e2d340204fd79d37720c9eda0
#              compressed-tensors NVFP4 (W4A4, group_size 16)
# Image:       vllm-spark:solar-open2-nvfp4-v0251-upstage00907fc-rawg1-pread-b12xsw-r4-exp
#              expected image ID (must match on BOTH nodes):
#              sha256:ecb7bfe3978a5241c5c304d52ce91e061e22b750178d21a4ef7788a08e86e774
#              (NGC 26.05 -> r2 7e282758328c -> r3 001dcd2fb66d -> r4; vLLM
#              0.25.1 @752a3a504485, FlashInfer 0.6.15, Upstage overlay 00907fc,
#              raw-g1 KDA fix, ST_PREAD gate, B12X shared-workspace gate)
#              No image rebuild was performed for this promotion.
#
# Performance envelope (MI arc 2026-07-27, re-confirmed Gate 4 2026-08-08):
#   - c1 paired median ratio 0.9863 (parity class, CI includes 1.0)
#   - c2 paired median ratio 0.9702 — c2 may remain approximately 1-3% below
#     the v0.22.1 baseline (median difference ~ -2.98%; NOT full parity).
#   - Gate 4 matched cells (pp512/pp2048 x tg128 x c1/c4) vs v0.22.1: -0.8%
#     to -6.0%, within the 10% acceptance threshold; TTFT improved in both
#     matched c1 cells.
#   - The earlier single-instance 12-14% regression figure (AC arc) is
#     SUPERSEDED by the multi-instance MI evidence and must not be cited as
#     the current performance result.
#
# Rollback reference (PRESERVED, NOT automatically started):
#   image  vllm-spark:solar-open2-nvfp4-v022d568-vllm0221-upstage00907fc-ecfix-exp
#          sha256:1873d2174691f67e16b5588fcef01680d21f1e7b42ac5587bd23d7503cae1366
#   preset presets/solar-open2-250b-nota-nvfp4-v022-kv4g-di-matched-tp2.env
#   Rollback validated end-to-end (Gate 6, 2026-08-08/09): stop -> start
#   rollback -> verify -> smoke-test -> stop rollback -> restore production
#   -> verify full state match. See docs/solar-open2-production.md section 5
#   for the exact operational rollback procedure, including the required
#   physical-reboot steps observed during validation.
#   On production failure: preserve evidence, stop the runtime cleanly,
#   leave ports 8000/6379 free, leave the cluster stopped, and REPORT. Do
#   not start v0.22.1 or any other model automatically.
#
# Runtime values below are byte-identical to the validated active-test
# preset presets/solar-open2-250b-nota-nvfp4-v0251-r4-active-test-tp2.env
# and to presets/solar-open2-250b-nota-nvfp4-v0251-r4-b12xsw-kv4g-exp-tp2.env
# (sha256 d8285a4162e5...) — zero functional difference introduced by this
# promotion. Requires overlays: docker-compose.solar-open2-hc-exp.yml +
# docker-compose.pread-r3.yml + docker-compose.b12xsw-r4.yml +
# docker-compose.b12x-cache.yml (warm B12X_CACHE_DIR per node, exported by
# the launcher — see docs/solar-open2-production.md section 5 for the exact
# launch command).
# Validated envelope: startup 457-501 s (cold, no page cache); KV 66,764
# tokens (bf16, 4 GiB/rank); engine-ready torch alloc ~76.2 GiB;
# shared-workspace 3 entries/rank, fallback 0; swap ~0; min MemAvailable
# ~21 GiB; eager only (no CUDA graphs).
#
# Known non-blocking operational caveats (see docs/solar-open2-production.md
# section 6 for full detail): host root filesystem pressure (~97-99% full
# during validation, Ray raylet emits a >95%-full warning, non-fatal);
# in-container `ray status` CLI may segfault (exit 139) even when the Ray
# cluster is healthy 2/2 — do not use it as the sole health check.
#
# E4M3 KV calibration is a SEPARATE, EXPERIMENTAL artifact
# (status: artifact-generated-not-runtime-validated). It is NOT part of this
# production preset. Production KV remains BF16.
# =============================================================================
VLLM_IMAGE=vllm-spark:solar-open2-nvfp4-v0251-upstage00907fc-rawg1-pread-b12xsw-r4-exp
ENTRYPOINT_FILE=./entrypoints/entrypoint.sh
MODEL_PATH=/home/bjk110/Documents/Models/upstage/nota-ai_Solar-Open2-250B-Nota-NVFP4
MODEL_CONTAINER_PATH=/models/Solar-Open2-250B-Nota-NVFP4
SERVED_MODEL_NAME=solar-open2-250b
CLUSTER_MODE=dual-rdma
TP_SIZE=2
HEAD_ROCE_IP=10.10.10.1
WORKER_ROCE_IP=10.10.10.2
ROCE_IF_NAME=enp1s0f0np0
IB_HCA_NAME=rocep1s0f0
DISTRIBUTED_BACKEND=ray
RAY_PORT=6379
HOST_PORT=8000
MAX_MODEL_LEN=4096
MAX_NUM_SEQS=8
GPU_MEMORY_UTILIZATION=0.80
MAX_NUM_BATCHED_TOKENS=2048
VLLM_EXTRA_ARGS=--enforce-eager --kv-cache-memory-bytes 4294967296 --moe-backend flashinfer_b12x --default-chat-template-kwargs {"think_render_option":"preserved"} --reasoning-parser solar_open2 --tool-call-parser solar_open2 --enable-auto-tool-choice --logits-processors vllm.v1.sample.logits_processor.solar_open2:SolarOpen2TemplateLogitsProcessor
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
NCCL_IB_DISABLE=0
NCCL_DEBUG=WARN
NCCL_NVLS_ENABLE=0
VLLM_USE_BREAKABLE_CUDAGRAPH=0
FLASHINFER_CUDA_ARCH_LIST=12.1
VLLM_NCCL_SO_PATH=/usr/lib/aarch64-linux-gnu/libnccl.so.2

# Cap the Ray object store at 4 GiB (default = 30% of RAM ~ 36 GiB) so the
# plasma store cannot compete with weight loading for unified memory.
RAY_OBJECT_STORE_MEMORY_BYTES=4294967296

# Lazy safetensors loader backend="pread" (validated loader-swap mitigation;
# requires docker-compose.pread-r3.yml overlay to reach the containers).
VLLM_SPARK_ST_PREAD=1

# Share FlashInfer b12x scratch workspaces across MoE layers (validated for
# eager b12x, single stream; ~27.9 GiB/rank -> ~0.6 GiB; requires
# docker-compose.b12xsw-r4.yml overlay to reach the containers).
VLLM_SPARK_B12X_SHARED_WORKSPACE=1
